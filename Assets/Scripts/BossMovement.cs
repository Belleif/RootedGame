using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BossMovement : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
/* This script should handle boss' AI pathfinding
 * Make it trigger after the player enters the bosses island / Make seperate script that handles the detection of player in boss island with collision detection
 * Make basic following pattern for now unless the boss needs to be stationary */
